"""
Shared User-Agent identification for every HTTP/WebSocket request this client
makes, so it's identifiable in server logs as automated rather than a browser.
"""

USER_AGENT = "UberSDR/Callsign_Scanner"
